{% extends "layout.njk" %}

{% block content %}
  <h1>{{pokemon.name}}</h1>
  <div>
    <h2>Possible moves</h2>
    <ul>
      {% for move in pokemon.moves %}
        <li>{{move.move.name}}</li>
      {% endfor %}
    </ul>
  </div>
{% endblock %}
